CREATE TABLE `ModeChoice` (
	mode INTEGER NOT NULL, 
	ttme INTEGER NOT NULL, 
	invc INTEGER NOT NULL, 
	invt INTEGER NOT NULL, 
	gc INTEGER NOT NULL, 
	hinc INTEGER NOT NULL, 
	psize INTEGER NOT NULL
);
