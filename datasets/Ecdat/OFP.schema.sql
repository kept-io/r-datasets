CREATE TABLE `OFP` (
	ofp INTEGER NOT NULL, 
	ofnp INTEGER NOT NULL, 
	opp INTEGER NOT NULL, 
	opnp INTEGER NOT NULL, 
	emr INTEGER NOT NULL, 
	hosp INTEGER NOT NULL, 
	numchron INTEGER NOT NULL, 
	adldiff INTEGER NOT NULL, 
	age FLOAT NOT NULL, 
	black BOOL NOT NULL, 
	sex VARCHAR(6) NOT NULL, 
	maried BOOL NOT NULL, 
	school INTEGER NOT NULL, 
	faminc FLOAT NOT NULL, 
	employed BOOL NOT NULL, 
	privins BOOL NOT NULL, 
	medicaid BOOL NOT NULL, 
	region VARCHAR(7) NOT NULL, 
	hlth VARCHAR(9) NOT NULL, 
	CHECK (black IN (0, 1)), 
	CHECK (maried IN (0, 1)), 
	CHECK (employed IN (0, 1)), 
	CHECK (privins IN (0, 1)), 
	CHECK (medicaid IN (0, 1))
);
