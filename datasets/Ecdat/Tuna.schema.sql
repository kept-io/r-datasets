CREATE TABLE `Tuna` (
	`Tuna.hid` INTEGER NOT NULL, 
	`Tuna.id` INTEGER NOT NULL, 
	`Tuna.choice` VARCHAR(4) NOT NULL, 
	`price.skw` FLOAT NOT NULL, 
	`price.cosw` FLOAT NOT NULL, 
	`price.sko` FLOAT NOT NULL, 
	`price.coso` FLOAT NOT NULL, 
	`price.pw` FLOAT NOT NULL
);
