CREATE TABLE `BudgetUK` (
	wfood FLOAT NOT NULL, 
	wfuel FLOAT NOT NULL, 
	wcloth FLOAT NOT NULL, 
	walc FLOAT NOT NULL, 
	wtrans FLOAT NOT NULL, 
	wother FLOAT NOT NULL, 
	totexp INTEGER NOT NULL, 
	income INTEGER NOT NULL, 
	age INTEGER NOT NULL, 
	children INTEGER NOT NULL
);
