CREATE TABLE `MCAS` (
	code INTEGER NOT NULL, 
	municipa VARCHAR(18) NOT NULL, 
	district VARCHAR(18) NOT NULL, 
	regday INTEGER NOT NULL, 
	specneed FLOAT NOT NULL, 
	bilingua INTEGER NOT NULL, 
	occupday INTEGER NOT NULL, 
	totday INTEGER NOT NULL, 
	spc FLOAT, 
	speced FLOAT NOT NULL, 
	lnchpct FLOAT NOT NULL, 
	tchratio FLOAT NOT NULL, 
	percap FLOAT NOT NULL, 
	totsc4 INTEGER NOT NULL, 
	totsc8 INTEGER, 
	avgsalary FLOAT, 
	pctel FLOAT NOT NULL
);
