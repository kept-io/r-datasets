CREATE TABLE solder (
	`Opening` VARCHAR(1) NOT NULL, 
	`Solder` VARCHAR(5) NOT NULL, 
	`Mask` VARCHAR(4) NOT NULL, 
	`PadType` VARCHAR(2) NOT NULL, 
	`Panel` INTEGER NOT NULL, 
	skips INTEGER NOT NULL
);
