CREATE TABLE infert (
	education VARCHAR(7) NOT NULL, 
	age INTEGER NOT NULL, 
	parity INTEGER NOT NULL, 
	induced INTEGER NOT NULL, 
	`case` INTEGER NOT NULL, 
	spontaneous INTEGER NOT NULL, 
	stratum INTEGER NOT NULL, 
	`pooled.stratum` INTEGER NOT NULL
);
