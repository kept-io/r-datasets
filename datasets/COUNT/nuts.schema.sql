CREATE TABLE nuts (
	cones INTEGER NOT NULL, 
	ntrees INTEGER NOT NULL, 
	dbh FLOAT NOT NULL, 
	height FLOAT NOT NULL, 
	cover FLOAT NOT NULL, 
	sntrees FLOAT NOT NULL, 
	sheight FLOAT NOT NULL, 
	scover FLOAT NOT NULL
);
