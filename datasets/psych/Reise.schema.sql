CREATE TABLE `Reise` (
	phone FLOAT NOT NULL, 
	routine FLOAT NOT NULL, 
	illness FLOAT NOT NULL, 
	listen FLOAT NOT NULL, 
	`explain` FLOAT NOT NULL, 
	respect FLOAT NOT NULL, 
	time FLOAT NOT NULL, 
	courtesy FLOAT NOT NULL, 
	helpful FLOAT NOT NULL, 
	happy FLOAT NOT NULL, 
	referral FLOAT NOT NULL, 
	necessary FLOAT NOT NULL, 
	delay FLOAT NOT NULL, 
	problem FLOAT NOT NULL, 
	help FLOAT NOT NULL, 
	paperwork FLOAT NOT NULL
);
