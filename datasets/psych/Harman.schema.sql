CREATE TABLE `Harman` (
	`Lewis` FLOAT NOT NULL, 
	`Roosevelt` FLOAT NOT NULL, 
	`Party Voting` FLOAT NOT NULL, 
	`Median Rental` FLOAT NOT NULL, 
	`Homeownership` FLOAT NOT NULL, 
	`Unemployment` FLOAT NOT NULL, 
	`Mobility` FLOAT NOT NULL, 
	`Education` FLOAT NOT NULL
);
