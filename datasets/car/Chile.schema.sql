CREATE TABLE `Chile` (
	region VARCHAR(2) NOT NULL, 
	population INTEGER NOT NULL, 
	sex VARCHAR(1) NOT NULL, 
	age INTEGER, 
	education VARCHAR(4), 
	income INTEGER, 
	statusquo FLOAT, 
	vote VARCHAR(4)
);
