CREATE TABLE `Highway1` (
	rate FLOAT NOT NULL, 
	len FLOAT NOT NULL, 
	`ADT` INTEGER NOT NULL, 
	trks INTEGER NOT NULL, 
	sigs1 FLOAT NOT NULL, 
	slim INTEGER NOT NULL, 
	shld INTEGER NOT NULL, 
	lane INTEGER NOT NULL, 
	acpt FLOAT NOT NULL, 
	itg FLOAT NOT NULL, 
	lwid INTEGER NOT NULL, 
	hwy VARCHAR(3) NOT NULL
);
