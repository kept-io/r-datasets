CREATE TABLE `Salaries` (
	rank VARCHAR(9) NOT NULL, 
	discipline VARCHAR(1) NOT NULL, 
	`yrs.since.phd` INTEGER NOT NULL, 
	`yrs.service` INTEGER NOT NULL, 
	sex VARCHAR(6) NOT NULL, 
	salary INTEGER NOT NULL
);
