CREATE TABLE `CrohnD` (
	`ID` INTEGER NOT NULL, 
	`nrAdvE` INTEGER NOT NULL, 
	`BMI` FLOAT NOT NULL, 
	height INTEGER NOT NULL, 
	country VARCHAR(2) NOT NULL, 
	sex VARCHAR(1) NOT NULL, 
	age INTEGER NOT NULL, 
	weight INTEGER NOT NULL, 
	treat VARCHAR(7) NOT NULL
);
