CREATE TABLE cpus (
	name VARCHAR(22) NOT NULL, 
	syct INTEGER NOT NULL, 
	mmin INTEGER NOT NULL, 
	mmax INTEGER NOT NULL, 
	cach INTEGER NOT NULL, 
	chmin INTEGER NOT NULL, 
	chmax INTEGER NOT NULL, 
	perf INTEGER NOT NULL, 
	estperf INTEGER NOT NULL
);
