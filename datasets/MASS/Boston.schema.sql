CREATE TABLE `Boston` (
	crim FLOAT NOT NULL, 
	zn FLOAT NOT NULL, 
	indus FLOAT NOT NULL, 
	chas INTEGER NOT NULL, 
	nox FLOAT NOT NULL, 
	rm FLOAT NOT NULL, 
	age FLOAT NOT NULL, 
	dis FLOAT NOT NULL, 
	rad INTEGER NOT NULL, 
	tax INTEGER NOT NULL, 
	ptratio FLOAT NOT NULL, 
	black FLOAT NOT NULL, 
	lstat FLOAT NOT NULL, 
	medv FLOAT NOT NULL
);
