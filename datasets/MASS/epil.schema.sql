CREATE TABLE epil (
	y INTEGER NOT NULL, 
	trt VARCHAR(9) NOT NULL, 
	base INTEGER NOT NULL, 
	age INTEGER NOT NULL, 
	`V4` INTEGER NOT NULL, 
	subject INTEGER NOT NULL, 
	period INTEGER NOT NULL, 
	lbase FLOAT NOT NULL, 
	lage FLOAT NOT NULL
);
