CREATE TABLE `VA` (
	stime INTEGER NOT NULL, 
	status INTEGER NOT NULL, 
	treat INTEGER NOT NULL, 
	age INTEGER NOT NULL, 
	`Karn` INTEGER NOT NULL, 
	`diag.time` INTEGER NOT NULL, 
	cell INTEGER NOT NULL, 
	prior INTEGER NOT NULL
);
