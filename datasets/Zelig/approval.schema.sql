CREATE TABLE approval (
	month INTEGER NOT NULL, 
	year INTEGER NOT NULL, 
	approve FLOAT NOT NULL, 
	disapprove FLOAT NOT NULL, 
	unsure FLOAT NOT NULL, 
	`sept.oct.2001` INTEGER NOT NULL, 
	`iraq.war` INTEGER NOT NULL, 
	`avg.price` FLOAT NOT NULL
);
