CREATE TABLE friendship (
	friends VARCHAR(32) NOT NULL, 
	advice VARCHAR(32) NOT NULL, 
	prestige VARCHAR(32) NOT NULL, 
	authority VARCHAR(32) NOT NULL, 
	perpower VARCHAR(32) NOT NULL, 
	per VARCHAR(32) NOT NULL, 
	count VARCHAR(32) NOT NULL
);
