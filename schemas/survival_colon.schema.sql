CREATE TABLE survival_colon (
	id INTEGER NOT NULL, 
	study INTEGER NOT NULL, 
	rx VARCHAR(7) NOT NULL, 
	sex INTEGER NOT NULL, 
	age INTEGER NOT NULL, 
	obstruct INTEGER NOT NULL, 
	perfor INTEGER NOT NULL, 
	adhere INTEGER NOT NULL, 
	nodes INTEGER, 
	status INTEGER NOT NULL, 
	differ INTEGER, 
	extent INTEGER NOT NULL, 
	surg INTEGER NOT NULL, 
	node4 INTEGER NOT NULL, 
	time INTEGER NOT NULL, 
	etype INTEGER NOT NULL
);
