CREATE TABLE `Ecdat_BudgetItaly` (
	wfood FLOAT NOT NULL, 
	whouse FLOAT NOT NULL, 
	wmisc FLOAT NOT NULL, 
	pfood FLOAT NOT NULL, 
	phouse FLOAT NOT NULL, 
	pmisc FLOAT NOT NULL, 
	totexp FLOAT NOT NULL, 
	year INTEGER NOT NULL, 
	income INTEGER NOT NULL, 
	size INTEGER NOT NULL, 
	pct FLOAT NOT NULL
);
