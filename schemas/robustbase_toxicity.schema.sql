CREATE TABLE robustbase_toxicity (
	toxicity FLOAT NOT NULL, 
	`logKow` FLOAT NOT NULL, 
	`pKa` FLOAT NOT NULL, 
	`ELUMO` FLOAT NOT NULL, 
	`Ecarb` FLOAT NOT NULL, 
	`Emet` FLOAT NOT NULL, 
	`RM` FLOAT NOT NULL, 
	`IR` FLOAT NOT NULL, 
	`Ts` FLOAT NOT NULL, 
	`P` FLOAT NOT NULL
);
