CREATE TABLE `COUNT_fasttrakg` (
	die INTEGER NOT NULL, 
	cases INTEGER NOT NULL, 
	anterior INTEGER NOT NULL, 
	hcabg INTEGER NOT NULL, 
	killip INTEGER NOT NULL, 
	kk1 INTEGER NOT NULL, 
	kk2 INTEGER NOT NULL, 
	kk3 INTEGER NOT NULL, 
	kk4 INTEGER NOT NULL
);
