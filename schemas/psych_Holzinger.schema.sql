CREATE TABLE `psych_Holzinger` (
	`T1` FLOAT NOT NULL, 
	`T2` FLOAT NOT NULL, 
	`T3.4` FLOAT NOT NULL, 
	`T6` FLOAT NOT NULL, 
	`T28` FLOAT NOT NULL, 
	`T29` FLOAT NOT NULL, 
	`T32` FLOAT NOT NULL, 
	`T34` FLOAT NOT NULL, 
	`T35` FLOAT NOT NULL, 
	`T36a` FLOAT NOT NULL, 
	`T13` FLOAT NOT NULL, 
	`T18` FLOAT NOT NULL, 
	`T25b` FLOAT NOT NULL, 
	`T77` FLOAT NOT NULL
);
