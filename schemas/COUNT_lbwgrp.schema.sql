CREATE TABLE `COUNT_lbwgrp` (
	lowbw INTEGER NOT NULL, 
	cases INTEGER NOT NULL, 
	smoke INTEGER NOT NULL, 
	race1 INTEGER NOT NULL, 
	race2 INTEGER NOT NULL, 
	race3 INTEGER NOT NULL, 
	low INTEGER NOT NULL
);
