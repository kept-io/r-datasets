CREATE TABLE boot_nuclear (
	cost FLOAT NOT NULL, 
	date FLOAT NOT NULL, 
	t1 INTEGER NOT NULL, 
	t2 INTEGER NOT NULL, 
	cap INTEGER NOT NULL, 
	pr INTEGER NOT NULL, 
	ne INTEGER NOT NULL, 
	ct INTEGER NOT NULL, 
	bw INTEGER NOT NULL, 
	`cum.n` INTEGER NOT NULL, 
	pt INTEGER NOT NULL
);
