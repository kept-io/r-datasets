CREATE TABLE `HistData_Guerry` (
	dept INTEGER NOT NULL, 
	`Region` VARCHAR(4), 
	`Department` VARCHAR(19) NOT NULL, 
	`Crime_pers` INTEGER NOT NULL, 
	`Crime_prop` INTEGER NOT NULL, 
	`Literacy` INTEGER NOT NULL, 
	`Donations` INTEGER NOT NULL, 
	`Infants` INTEGER NOT NULL, 
	`Suicides` INTEGER NOT NULL, 
	`MainCity` VARCHAR(5) NOT NULL, 
	`Wealth` INTEGER NOT NULL, 
	`Commerce` INTEGER NOT NULL, 
	`Clergy` INTEGER NOT NULL, 
	`Crime_parents` INTEGER NOT NULL, 
	`Infanticide` INTEGER NOT NULL, 
	`Donation_clergy` INTEGER NOT NULL, 
	`Lottery` INTEGER NOT NULL, 
	`Desertion` INTEGER NOT NULL, 
	`Instruction` INTEGER NOT NULL, 
	`Prostitutes` INTEGER NOT NULL, 
	`Distance` FLOAT NOT NULL, 
	`Area` INTEGER NOT NULL, 
	`Pop1831` FLOAT NOT NULL
);
