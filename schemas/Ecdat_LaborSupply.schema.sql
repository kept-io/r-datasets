CREATE TABLE `Ecdat_LaborSupply` (
	lnhr FLOAT NOT NULL, 
	lnwg FLOAT NOT NULL, 
	kids INTEGER NOT NULL, 
	age INTEGER NOT NULL, 
	disab INTEGER NOT NULL, 
	id INTEGER NOT NULL, 
	year INTEGER NOT NULL
);
