CREATE TABLE `car_Ericksen` (
	minority FLOAT NOT NULL, 
	crime INTEGER NOT NULL, 
	poverty FLOAT NOT NULL, 
	language FLOAT NOT NULL, 
	highschool FLOAT NOT NULL, 
	housing FLOAT NOT NULL, 
	city VARCHAR(5) NOT NULL, 
	conventional INTEGER NOT NULL, 
	undercount FLOAT NOT NULL
);
