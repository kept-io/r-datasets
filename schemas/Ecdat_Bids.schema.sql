CREATE TABLE `Ecdat_Bids` (
	docno INTEGER NOT NULL, 
	weeks FLOAT NOT NULL, 
	numbids INTEGER NOT NULL, 
	takeover INTEGER NOT NULL, 
	bidprem FLOAT NOT NULL, 
	insthold FLOAT NOT NULL, 
	size FLOAT NOT NULL, 
	leglrest INTEGER NOT NULL, 
	rearest INTEGER NOT NULL, 
	finrest INTEGER NOT NULL, 
	regulatn INTEGER NOT NULL, 
	whtknght INTEGER NOT NULL
);
