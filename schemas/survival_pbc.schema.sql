CREATE TABLE survival_pbc (
	id INTEGER NOT NULL, 
	time INTEGER NOT NULL, 
	status INTEGER NOT NULL, 
	trt INTEGER, 
	age FLOAT NOT NULL, 
	sex VARCHAR(1) NOT NULL, 
	ascites INTEGER, 
	hepato INTEGER, 
	spiders INTEGER, 
	edema FLOAT NOT NULL, 
	bili FLOAT NOT NULL, 
	chol INTEGER, 
	albumin FLOAT NOT NULL, 
	copper INTEGER, 
	`alk.phos` FLOAT, 
	ast FLOAT, 
	trig INTEGER, 
	platelet INTEGER, 
	protime FLOAT, 
	stage INTEGER
);
