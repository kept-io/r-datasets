CREATE TABLE psych_cities (
	`ATL` INTEGER NOT NULL, 
	`BOS` INTEGER NOT NULL, 
	`ORD` INTEGER NOT NULL, 
	`DCA` INTEGER NOT NULL, 
	`DEN` INTEGER NOT NULL, 
	`LAX` INTEGER NOT NULL, 
	`MIA` INTEGER NOT NULL, 
	`JFK` INTEGER NOT NULL, 
	`SEA` INTEGER NOT NULL, 
	`SFO` INTEGER NOT NULL, 
	`MSY` INTEGER NOT NULL
);
