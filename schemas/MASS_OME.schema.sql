CREATE TABLE `MASS_OME` (
	`ID` INTEGER NOT NULL, 
	`Age` INTEGER NOT NULL, 
	`OME` VARCHAR(4), 
	`Loud` INTEGER NOT NULL, 
	`Noise` VARCHAR(10) NOT NULL, 
	`Correct` INTEGER NOT NULL, 
	`Trials` INTEGER NOT NULL
);
