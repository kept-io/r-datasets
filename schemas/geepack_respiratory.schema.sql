CREATE TABLE geepack_respiratory (
	center INTEGER NOT NULL, 
	id INTEGER NOT NULL, 
	treat VARCHAR(1) NOT NULL, 
	sex VARCHAR(1) NOT NULL, 
	age INTEGER NOT NULL, 
	baseline INTEGER NOT NULL, 
	visit INTEGER NOT NULL, 
	outcome INTEGER NOT NULL
);
