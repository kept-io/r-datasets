CREATE TABLE datasets_longley (
	`GNP.deflator` FLOAT NOT NULL, 
	`GNP` FLOAT NOT NULL, 
	`Unemployed` FLOAT NOT NULL, 
	`Armed.Forces` FLOAT NOT NULL, 
	`Population` FLOAT NOT NULL, 
	`Year` INTEGER NOT NULL, 
	`Employed` FLOAT NOT NULL
);
