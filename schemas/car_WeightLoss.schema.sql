CREATE TABLE `car_WeightLoss` (
	`group` VARCHAR(7) NOT NULL, 
	wl1 INTEGER NOT NULL, 
	wl2 INTEGER NOT NULL, 
	wl3 INTEGER NOT NULL, 
	se1 INTEGER NOT NULL, 
	se2 INTEGER NOT NULL, 
	se3 INTEGER NOT NULL
);
