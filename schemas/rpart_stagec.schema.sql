CREATE TABLE rpart_stagec (
	pgtime FLOAT NOT NULL, 
	pgstat INTEGER NOT NULL, 
	age INTEGER NOT NULL, 
	eet INTEGER, 
	g2 FLOAT, 
	grade INTEGER NOT NULL, 
	gleason INTEGER, 
	ploidy VARCHAR(10) NOT NULL
);
