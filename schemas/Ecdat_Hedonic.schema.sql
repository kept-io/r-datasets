CREATE TABLE `Ecdat_Hedonic` (
	mv FLOAT NOT NULL, 
	crim FLOAT NOT NULL, 
	zn FLOAT NOT NULL, 
	indus FLOAT NOT NULL, 
	chas BOOL NOT NULL, 
	nox FLOAT NOT NULL, 
	rm FLOAT NOT NULL, 
	age FLOAT NOT NULL, 
	dis FLOAT NOT NULL, 
	rad FLOAT NOT NULL, 
	tax INTEGER NOT NULL, 
	ptratio FLOAT NOT NULL, 
	blacks FLOAT NOT NULL, 
	lstat FLOAT NOT NULL, 
	townid INTEGER NOT NULL, 
	CHECK (chas IN (0, 1))
);
