CREATE TABLE `Zelig_voteincome` (
	state VARCHAR(2) NOT NULL, 
	year INTEGER NOT NULL, 
	vote INTEGER NOT NULL, 
	income INTEGER NOT NULL, 
	education INTEGER NOT NULL, 
	age INTEGER NOT NULL, 
	female INTEGER NOT NULL
);
