CREATE TABLE `Ecdat_Mode` (
	choice VARCHAR(7) NOT NULL, 
	`cost.car` FLOAT NOT NULL, 
	`cost.carpool` FLOAT NOT NULL, 
	`cost.bus` FLOAT NOT NULL, 
	`cost.rail` FLOAT NOT NULL, 
	`time.car` FLOAT NOT NULL, 
	`time.carpool` FLOAT NOT NULL, 
	`time.bus` FLOAT NOT NULL, 
	`time.rail` FLOAT NOT NULL
);
