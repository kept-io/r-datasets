CREATE TABLE robustbase_heart (
	start FLOAT NOT NULL, 
	stop FLOAT NOT NULL, 
	event INTEGER NOT NULL, 
	age FLOAT NOT NULL, 
	year FLOAT NOT NULL, 
	surgery INTEGER NOT NULL, 
	transplant INTEGER NOT NULL, 
	id INTEGER NOT NULL
);
