CREATE TABLE `car_Soils` (
	`Group` INTEGER NOT NULL, 
	`Contour` VARCHAR(10) NOT NULL, 
	`Depth` VARCHAR(5) NOT NULL, 
	`Gp` VARCHAR(2) NOT NULL, 
	`Block` INTEGER NOT NULL, 
	`pH` FLOAT NOT NULL, 
	`N` FLOAT NOT NULL, 
	`Dens` FLOAT NOT NULL, 
	`P` INTEGER NOT NULL, 
	`Ca` FLOAT NOT NULL, 
	`Mg` FLOAT NOT NULL, 
	`K` FLOAT NOT NULL, 
	`Na` FLOAT NOT NULL, 
	`Conduc` FLOAT NOT NULL
);
