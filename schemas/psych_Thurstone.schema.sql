CREATE TABLE `psych_Thurstone` (
	`Sentences` FLOAT NOT NULL, 
	`Vocabulary` FLOAT NOT NULL, 
	`Sent.Completion` FLOAT NOT NULL, 
	`First.Letters` FLOAT NOT NULL, 
	`4.Letter.Words` FLOAT NOT NULL, 
	`Suffixes` FLOAT NOT NULL, 
	`Letter.Series` FLOAT NOT NULL, 
	`Pedigrees` FLOAT NOT NULL, 
	`Letter.Group` FLOAT NOT NULL
);
