CREATE TABLE `car_States` (
	region VARCHAR(3) NOT NULL, 
	pop INTEGER NOT NULL, 
	`SATV` INTEGER NOT NULL, 
	`SATM` INTEGER NOT NULL, 
	percent INTEGER NOT NULL, 
	dollars FLOAT NOT NULL, 
	pay INTEGER NOT NULL
);
