CREATE TABLE `sem_Klein` (
	`Year` INTEGER NOT NULL, 
	`C` FLOAT NOT NULL, 
	`P` FLOAT NOT NULL, 
	`Wp` FLOAT NOT NULL, 
	`I` FLOAT NOT NULL, 
	`K.lag` FLOAT NOT NULL, 
	`X` FLOAT NOT NULL, 
	`Wg` FLOAT NOT NULL, 
	`G` FLOAT NOT NULL, 
	`T` FLOAT NOT NULL
);
