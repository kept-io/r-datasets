CREATE TABLE `Ecdat_Clothing` (
	tsales INTEGER NOT NULL, 
	sales FLOAT NOT NULL, 
	margin FLOAT NOT NULL, 
	nown FLOAT NOT NULL, 
	nfull FLOAT NOT NULL, 
	npart FLOAT NOT NULL, 
	naux FLOAT NOT NULL, 
	hoursw INTEGER NOT NULL, 
	hourspw FLOAT NOT NULL, 
	inv1 FLOAT NOT NULL, 
	inv2 FLOAT NOT NULL, 
	ssize INTEGER NOT NULL, 
	start FLOAT NOT NULL
);
