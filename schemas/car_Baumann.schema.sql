CREATE TABLE `car_Baumann` (
	`group` VARCHAR(5) NOT NULL, 
	`pretest.1` INTEGER NOT NULL, 
	`pretest.2` INTEGER NOT NULL, 
	`post.test.1` INTEGER NOT NULL, 
	`post.test.2` INTEGER NOT NULL, 
	`post.test.3` INTEGER NOT NULL
);
