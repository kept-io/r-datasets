CREATE TABLE psych_cushny (
	`Control` FLOAT NOT NULL, 
	drug1 FLOAT NOT NULL, 
	`drug2L` FLOAT NOT NULL, 
	`drug2R` FLOAT NOT NULL, 
	delta1 FLOAT NOT NULL, 
	`delta2L` FLOAT NOT NULL, 
	`delta2R` FLOAT NOT NULL
);
