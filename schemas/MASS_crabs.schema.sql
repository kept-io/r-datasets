CREATE TABLE `MASS_crabs` (
	sp VARCHAR(1) NOT NULL, 
	sex VARCHAR(1) NOT NULL, 
	`index` INTEGER NOT NULL, 
	`FL` FLOAT NOT NULL, 
	`RW` FLOAT NOT NULL, 
	`CL` FLOAT NOT NULL, 
	`CW` FLOAT NOT NULL, 
	`BD` FLOAT NOT NULL
);
